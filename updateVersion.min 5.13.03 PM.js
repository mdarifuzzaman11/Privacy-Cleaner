const fs=require("fs"),path=require("path"),manifestPath=path.join(__dirname,"manifest.json"),manifest=require(manifestPath);let versionParts=manifest.version.split(".");versionParts[versionParts.length-1]=parseInt(versionParts[versionParts.length-1])+1,manifest.version=versionParts.join("."),fs.writeFileSync(manifestPath,JSON.stringify(manifest,null,2));